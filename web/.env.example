VITE_SERVER_API_URL=
# Optional: leave empty in production — Netlify proxies /api to Railway.
# For local dev, leave empty too (Vite proxies /api to localhost:3000).
VITE_API_AUTH_TOKEN=
VITE_MACHINE_BRAND=Usha
VITE_MACHINE_MODEL=Quick Stitch Master
